app = "kurdstream"
primary_region = "fra"

[build]

[http_service]
  internal_port = 5000
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

[mounts]
  source = "kurdstream_data"
  destination = "/app/data"

[env]
  DATA_DIR = "/app/data"
